---
- name: Pull the latest code changes, discarding any local changes.
  git:
    repo: "{{ git_repo_url }}"
    dest: "{{ repo_dest }}"
    version: "{{ gitbranch }}"
    update: yes
    force: true