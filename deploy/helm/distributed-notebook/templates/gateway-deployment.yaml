apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    io.kompose.service: gateway
    app: gateway
  name: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: gateway
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.26.0 (40646f47)
      creationTimestamp: null
      labels:
        io.kompose.service: gateway
        app: gateway
    spec:
      containers:
        - image: {{ .Values.gateway.image }}:{{ .Values.gateway.image_tag }}
          name: gateway
          resources: {}
          env:
            - name: SHARED_CONFIG_DIR
              value: "{{ .Values.common.config_file_dir }}"
            - name: IPYTHON_CONFIG_PATH
              value: {{ .Values.common.ipython_config_path }}
            - name: LOCAL_DAEMON_SERVICE_NAME
              value: {{ .Values.local_daemon.service_name }}
            - name: LOCAL_DAEMON_SERVICE_PORT
              value: "{{ .Values.local_daemon.service_port }}"
      restartPolicy: Always
      serviceAccountName: gateway-service-account
status: {}
