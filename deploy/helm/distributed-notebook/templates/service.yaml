apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributed-notebook.fullname" . }}
  labels:
    {{- include "distributed-notebook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distributed-notebook.selectorLabels" . | nindent 4 }}
