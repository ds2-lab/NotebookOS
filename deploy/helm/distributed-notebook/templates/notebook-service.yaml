apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    io.kompose.service: jupyter-notebook
  name: jupyter-notebook
spec:
  ports:
    - name: "{{ .Values.notebook_server.port }}"
      port: {{ .Values.notebook_server.port }}
      targetPort: {{ .Values.notebook_server.port }}
  selector:
    io.kompose.service: jupyter-notebook
status:
  loadBalancer: {}
